//
// Copyright (c) 2018 Volterra, Inc. All rights reserved.
// Code generated by ves-gen-tf-provider. DO NOT EDIT.
//

package volterra

import (
	"context"
	"fmt"
	"log"
	"strings"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"

	"gopkg.volterra.us/stdlib/client/vesapi"

	ves_io_schema "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema"
	ves_io_schema_vs_profiles "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/vs_profiles"
	ves_io_schema_vs_profiles_services_quic_profile "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/vs_profiles/services/quic_profile"
)

// resourceVolterraQuicProfile is implementation of Volterra's QuicProfile resources
func resourceVolterraQuicProfile() *schema.Resource {
	return &schema.Resource{
		Create: resourceVolterraQuicProfileCreate,
		Read:   resourceVolterraQuicProfileRead,
		Update: resourceVolterraQuicProfileUpdate,
		Delete: resourceVolterraQuicProfileDelete,

		Schema: map[string]*schema.Schema{

			"annotations": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"description": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"disable": {
				Type:     schema.TypeBool,
				Optional: true,
			},

			"labels": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"name": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"namespace": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"app_service": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"bidi_concurrent_streams_per_connection": {
				Type:     schema.TypeInt,
				Optional: true,
			},

			"command": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"spin_bit": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"sub_path": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"uni_concurrent_streams_per_connection": {
				Type:     schema.TypeInt,
				Optional: true,
			},

			"user_spec": {

				Type: schema.TypeList,

				Optional: true,
				Elem: &schema.Schema{
					Type: schema.TypeString,
				},
			},
		},
	}
}

// resourceVolterraQuicProfileCreate creates QuicProfile resource
func resourceVolterraQuicProfileCreate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	createMeta := &ves_io_schema.ObjectCreateMetaType{}
	createSpec := &ves_io_schema_vs_profiles_services_quic_profile.CreateSpecType{}
	createReq := &ves_io_schema_vs_profiles_services_quic_profile.CreateRequest{
		Metadata: createMeta,
		Spec:     createSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		createMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		createMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		createMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		createMeta.Namespace =
			v.(string)
	}

	//app_service
	if v, ok := d.GetOk("app_service"); ok && !isIntfNil(v) {

		createSpec.AppService =
			v.(string)

	}

	//bidi_concurrent_streams_per_connection
	if v, ok := d.GetOk("bidi_concurrent_streams_per_connection"); ok && !isIntfNil(v) {

		createSpec.BidiConcurrentStreamsPerConnection =
			uint32(v.(int))

	}

	//command
	if v, ok := d.GetOk("command"); ok && !isIntfNil(v) {

		createSpec.Command =
			v.(string)

	}

	//spin_bit
	if v, ok := d.GetOk("spin_bit"); ok && !isIntfNil(v) {

		createSpec.SpinBit = ves_io_schema_vs_profiles.EnableDisableChoice(ves_io_schema_vs_profiles.EnableDisableChoice_value[v.(string)])

	}

	//sub_path
	if v, ok := d.GetOk("sub_path"); ok && !isIntfNil(v) {

		createSpec.SubPath =
			v.(string)

	}

	//uni_concurrent_streams_per_connection
	if v, ok := d.GetOk("uni_concurrent_streams_per_connection"); ok && !isIntfNil(v) {

		createSpec.UniConcurrentStreamsPerConnection =
			uint32(v.(int))

	}

	//user_spec
	if v, ok := d.GetOk("user_spec"); ok && !isIntfNil(v) {

		ls := make([]string, len(v.([]interface{})))
		for i, v := range v.([]interface{}) {
			if v == nil {
				return fmt.Errorf("please provide valid non-empty string value of field user_spec")
			}
			if str, ok := v.(string); ok {
				ls[i] = str
			}
		}
		createSpec.UserSpec = ls

	}

	log.Printf("[DEBUG] Creating Volterra QuicProfile object with struct: %+v", createReq)

	createQuicProfileResp, err := client.CreateObject(context.Background(), ves_io_schema_vs_profiles_services_quic_profile.ObjectType, createReq)
	if err != nil {
		return fmt.Errorf("error creating QuicProfile: %s", err)
	}
	d.SetId(createQuicProfileResp.GetObjSystemMetadata().GetUid())

	return resourceVolterraQuicProfileRead(d, meta)
}

func resourceVolterraQuicProfileRead(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	resp, err := client.GetObject(context.Background(), ves_io_schema_vs_profiles_services_quic_profile.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] QuicProfile %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra QuicProfile %q: %s", d.Id(), err)
	}

	return setQuicProfileFields(client, d, resp)
}

func setQuicProfileFields(client *APIClient, d *schema.ResourceData, resp vesapi.GetObjectResponse) error {
	metadata := resp.GetObjMetadata()

	d.Set("annotations", metadata.GetAnnotations())

	d.Set("description", metadata.GetDescription())

	d.Set("disable", metadata.GetDisable())

	d.Set("labels", metadata.GetLabels())

	d.Set("name", metadata.GetName())

	d.Set("namespace", metadata.GetNamespace())

	return nil
}

// resourceVolterraQuicProfileUpdate updates QuicProfile resource
func resourceVolterraQuicProfileUpdate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	updateMeta := &ves_io_schema.ObjectReplaceMetaType{}
	updateSpec := &ves_io_schema_vs_profiles_services_quic_profile.ReplaceSpecType{}
	updateReq := &ves_io_schema_vs_profiles_services_quic_profile.ReplaceRequest{
		Metadata: updateMeta,
		Spec:     updateSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		updateMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		updateMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		updateMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		updateMeta.Namespace =
			v.(string)
	}

	if v, ok := d.GetOk("app_service"); ok && !isIntfNil(v) {

		updateSpec.AppService =
			v.(string)

	}

	if v, ok := d.GetOk("bidi_concurrent_streams_per_connection"); ok && !isIntfNil(v) {

		updateSpec.BidiConcurrentStreamsPerConnection =
			uint32(v.(int))

	}

	if v, ok := d.GetOk("command"); ok && !isIntfNil(v) {

		updateSpec.Command =
			v.(string)

	}

	if v, ok := d.GetOk("spin_bit"); ok && !isIntfNil(v) {

		updateSpec.SpinBit = ves_io_schema_vs_profiles.EnableDisableChoice(ves_io_schema_vs_profiles.EnableDisableChoice_value[v.(string)])

	}

	if v, ok := d.GetOk("sub_path"); ok && !isIntfNil(v) {

		updateSpec.SubPath =
			v.(string)

	}

	if v, ok := d.GetOk("uni_concurrent_streams_per_connection"); ok && !isIntfNil(v) {

		updateSpec.UniConcurrentStreamsPerConnection =
			uint32(v.(int))

	}

	if v, ok := d.GetOk("user_spec"); ok && !isIntfNil(v) {

		ls := make([]string, len(v.([]interface{})))
		for i, v := range v.([]interface{}) {
			if v == nil {
				return fmt.Errorf("please provide valid non-empty string value of field user_spec")
			}
			if str, ok := v.(string); ok {
				ls[i] = str
			}
		}
		updateSpec.UserSpec = ls

	}

	log.Printf("[DEBUG] Updating Volterra QuicProfile obj with struct: %+v", updateReq)

	err := client.ReplaceObject(context.Background(), ves_io_schema_vs_profiles_services_quic_profile.ObjectType, updateReq)
	if err != nil {
		return fmt.Errorf("error updating QuicProfile: %s", err)
	}

	return resourceVolterraQuicProfileRead(d, meta)
}

func resourceVolterraQuicProfileDelete(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	_, err := client.GetObject(context.Background(), ves_io_schema_vs_profiles_services_quic_profile.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] QuicProfile %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra QuicProfile before deleting %q: %s", d.Id(), err)
	}

	log.Printf("[DEBUG] Deleting Volterra QuicProfile obj with name %+v in namespace %+v", name, namespace)
	opts := []vesapi.CallOpt{
		vesapi.WithFailIfReferred(),
	}

	err = client.DeleteObject(context.Background(), ves_io_schema_vs_profiles_services_quic_profile.ObjectType, namespace, name, opts...)
	if err != nil {
		return fmt.Errorf("error deleting QuicProfile: %w", err)
	}
	return nil

}
