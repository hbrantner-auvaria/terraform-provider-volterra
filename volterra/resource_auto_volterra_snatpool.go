//
// Copyright (c) 2018 Volterra, Inc. All rights reserved.
// Code generated by ves-gen-tf-provider. DO NOT EDIT.
//

package volterra

import (
	"context"
	"fmt"
	"log"
	"strings"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"

	"gopkg.volterra.us/stdlib/client/vesapi"

	ves_io_schema "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema"
	ves_io_schema_vs_profiles_address_translation_snatpool "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/vs_profiles/address_translation/snatpool"
)

// resourceVolterraSnatpool is implementation of Volterra's Snatpool resources
func resourceVolterraSnatpool() *schema.Resource {
	return &schema.Resource{
		Create: resourceVolterraSnatpoolCreate,
		Read:   resourceVolterraSnatpoolRead,
		Update: resourceVolterraSnatpoolUpdate,
		Delete: resourceVolterraSnatpoolDelete,

		Schema: map[string]*schema.Schema{

			"annotations": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"description": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"disable": {
				Type:     schema.TypeBool,
				Optional: true,
			},

			"labels": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"name": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"namespace": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"app_service": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"full_path": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"members": {

				Type: schema.TypeList,

				Required: true,
				Elem: &schema.Schema{
					Type: schema.TypeString,
				},
			},

			"sub_path": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"traffic_acceleration_status": {
				Type:     schema.TypeString,
				Optional: true,
			},
		},
	}
}

// resourceVolterraSnatpoolCreate creates Snatpool resource
func resourceVolterraSnatpoolCreate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	createMeta := &ves_io_schema.ObjectCreateMetaType{}
	createSpec := &ves_io_schema_vs_profiles_address_translation_snatpool.CreateSpecType{}
	createReq := &ves_io_schema_vs_profiles_address_translation_snatpool.CreateRequest{
		Metadata: createMeta,
		Spec:     createSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		createMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		createMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		createMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		createMeta.Namespace =
			v.(string)
	}

	//app_service
	if v, ok := d.GetOk("app_service"); ok && !isIntfNil(v) {

		createSpec.AppService =
			v.(string)

	}

	//full_path
	if v, ok := d.GetOk("full_path"); ok && !isIntfNil(v) {

		createSpec.FullPath =
			v.(string)

	}

	//members
	if v, ok := d.GetOk("members"); ok && !isIntfNil(v) {

		ls := make([]string, len(v.([]interface{})))
		for i, v := range v.([]interface{}) {
			if v == nil {
				return fmt.Errorf("please provide valid non-empty string value of field members")
			}
			if str, ok := v.(string); ok {
				ls[i] = str
			}
		}
		createSpec.Members = ls

	}

	//sub_path
	if v, ok := d.GetOk("sub_path"); ok && !isIntfNil(v) {

		createSpec.SubPath =
			v.(string)

	}

	//traffic_acceleration_status
	if v, ok := d.GetOk("traffic_acceleration_status"); ok && !isIntfNil(v) {

		createSpec.TrafficAccelerationStatus = ves_io_schema_vs_profiles_address_translation_snatpool.TrafficAccelerationStatusChoice(ves_io_schema_vs_profiles_address_translation_snatpool.TrafficAccelerationStatusChoice_value[v.(string)])

	}

	log.Printf("[DEBUG] Creating Volterra Snatpool object with struct: %+v", createReq)

	createSnatpoolResp, err := client.CreateObject(context.Background(), ves_io_schema_vs_profiles_address_translation_snatpool.ObjectType, createReq)
	if err != nil {
		return fmt.Errorf("error creating Snatpool: %s", err)
	}
	d.SetId(createSnatpoolResp.GetObjSystemMetadata().GetUid())

	return resourceVolterraSnatpoolRead(d, meta)
}

func resourceVolterraSnatpoolRead(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	resp, err := client.GetObject(context.Background(), ves_io_schema_vs_profiles_address_translation_snatpool.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] Snatpool %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra Snatpool %q: %s", d.Id(), err)
	}

	return setSnatpoolFields(client, d, resp)
}

func setSnatpoolFields(client *APIClient, d *schema.ResourceData, resp vesapi.GetObjectResponse) error {
	metadata := resp.GetObjMetadata()

	d.Set("annotations", metadata.GetAnnotations())

	d.Set("description", metadata.GetDescription())

	d.Set("disable", metadata.GetDisable())

	d.Set("labels", metadata.GetLabels())

	d.Set("name", metadata.GetName())

	d.Set("namespace", metadata.GetNamespace())

	return nil
}

// resourceVolterraSnatpoolUpdate updates Snatpool resource
func resourceVolterraSnatpoolUpdate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	updateMeta := &ves_io_schema.ObjectReplaceMetaType{}
	updateSpec := &ves_io_schema_vs_profiles_address_translation_snatpool.ReplaceSpecType{}
	updateReq := &ves_io_schema_vs_profiles_address_translation_snatpool.ReplaceRequest{
		Metadata: updateMeta,
		Spec:     updateSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		updateMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		updateMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		updateMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		updateMeta.Namespace =
			v.(string)
	}

	if v, ok := d.GetOk("app_service"); ok && !isIntfNil(v) {

		updateSpec.AppService =
			v.(string)

	}

	if v, ok := d.GetOk("full_path"); ok && !isIntfNil(v) {

		updateSpec.FullPath =
			v.(string)

	}

	if v, ok := d.GetOk("members"); ok && !isIntfNil(v) {

		ls := make([]string, len(v.([]interface{})))
		for i, v := range v.([]interface{}) {
			if v == nil {
				return fmt.Errorf("please provide valid non-empty string value of field members")
			}
			if str, ok := v.(string); ok {
				ls[i] = str
			}
		}
		updateSpec.Members = ls

	}

	if v, ok := d.GetOk("sub_path"); ok && !isIntfNil(v) {

		updateSpec.SubPath =
			v.(string)

	}

	if v, ok := d.GetOk("traffic_acceleration_status"); ok && !isIntfNil(v) {

		updateSpec.TrafficAccelerationStatus = ves_io_schema_vs_profiles_address_translation_snatpool.TrafficAccelerationStatusChoice(ves_io_schema_vs_profiles_address_translation_snatpool.TrafficAccelerationStatusChoice_value[v.(string)])

	}

	log.Printf("[DEBUG] Updating Volterra Snatpool obj with struct: %+v", updateReq)

	err := client.ReplaceObject(context.Background(), ves_io_schema_vs_profiles_address_translation_snatpool.ObjectType, updateReq)
	if err != nil {
		return fmt.Errorf("error updating Snatpool: %s", err)
	}

	return resourceVolterraSnatpoolRead(d, meta)
}

func resourceVolterraSnatpoolDelete(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	_, err := client.GetObject(context.Background(), ves_io_schema_vs_profiles_address_translation_snatpool.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] Snatpool %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra Snatpool before deleting %q: %s", d.Id(), err)
	}

	log.Printf("[DEBUG] Deleting Volterra Snatpool obj with name %+v in namespace %+v", name, namespace)
	opts := []vesapi.CallOpt{
		vesapi.WithFailIfReferred(),
	}

	err = client.DeleteObject(context.Background(), ves_io_schema_vs_profiles_address_translation_snatpool.ObjectType, namespace, name, opts...)
	if err != nil {
		return fmt.Errorf("error deleting Snatpool: %w", err)
	}
	return nil

}
