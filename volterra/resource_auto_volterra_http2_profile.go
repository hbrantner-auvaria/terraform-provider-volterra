//
// Copyright (c) 2018 Volterra, Inc. All rights reserved.
// Code generated by ves-gen-tf-provider. DO NOT EDIT.
//

package volterra

import (
	"context"
	"fmt"
	"log"
	"strings"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"

	"gopkg.volterra.us/stdlib/client/vesapi"

	ves_io_schema "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema"
	ves_io_schema_vs_profiles "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/vs_profiles"
	ves_io_schema_vs_profiles_services_http2_profile "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/vs_profiles/services/http2_profile"
)

// resourceVolterraHttp2Profile is implementation of Volterra's Http2Profile resources
func resourceVolterraHttp2Profile() *schema.Resource {
	return &schema.Resource{
		Create: resourceVolterraHttp2ProfileCreate,
		Read:   resourceVolterraHttp2ProfileRead,
		Update: resourceVolterraHttp2ProfileUpdate,
		Delete: resourceVolterraHttp2ProfileDelete,

		Schema: map[string]*schema.Schema{

			"annotations": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"description": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"disable": {
				Type:     schema.TypeBool,
				Optional: true,
			},

			"labels": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"name": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"namespace": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"activation_modes": {

				Type: schema.TypeList,

				Optional: true,
				Elem: &schema.Schema{
					Type: schema.TypeString,
				},
			},

			"app_service": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"command": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"concurrent_streams_per_connection": {
				Type:     schema.TypeInt,
				Optional: true,
			},

			"connection_idle_timeout": {
				Type:     schema.TypeInt,
				Optional: true,
			},

			"enforce_tls_requirements": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"frame_size": {
				Type:     schema.TypeInt,
				Optional: true,
			},

			"header_table_size": {
				Type:     schema.TypeInt,
				Optional: true,
			},

			"include_content_length": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"insert_header": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"insert_header_name": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"receive_window": {
				Type:     schema.TypeInt,
				Optional: true,
			},

			"user_spec": {

				Type: schema.TypeList,

				Optional: true,
				Elem: &schema.Schema{
					Type: schema.TypeString,
				},
			},

			"write_size": {
				Type:     schema.TypeInt,
				Optional: true,
			},
		},
	}
}

// resourceVolterraHttp2ProfileCreate creates Http2Profile resource
func resourceVolterraHttp2ProfileCreate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	createMeta := &ves_io_schema.ObjectCreateMetaType{}
	createSpec := &ves_io_schema_vs_profiles_services_http2_profile.CreateSpecType{}
	createReq := &ves_io_schema_vs_profiles_services_http2_profile.CreateRequest{
		Metadata: createMeta,
		Spec:     createSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		createMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		createMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		createMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		createMeta.Namespace =
			v.(string)
	}

	//activation_modes
	if v, ok := d.GetOk("activation_modes"); ok && !isIntfNil(v) {

		ls := make([]string, len(v.([]interface{})))
		for i, v := range v.([]interface{}) {
			if v == nil {
				return fmt.Errorf("please provide valid non-empty string value of field activation_modes")
			}
			if str, ok := v.(string); ok {
				ls[i] = str
			}
		}
		createSpec.ActivationModes = ls

	}

	//app_service
	if v, ok := d.GetOk("app_service"); ok && !isIntfNil(v) {

		createSpec.AppService =
			v.(string)

	}

	//command
	if v, ok := d.GetOk("command"); ok && !isIntfNil(v) {

		createSpec.Command =
			v.(string)

	}

	//concurrent_streams_per_connection
	if v, ok := d.GetOk("concurrent_streams_per_connection"); ok && !isIntfNil(v) {

		createSpec.ConcurrentStreamsPerConnection =
			uint32(v.(int))

	}

	//connection_idle_timeout
	if v, ok := d.GetOk("connection_idle_timeout"); ok && !isIntfNil(v) {

		createSpec.ConnectionIdleTimeout =
			uint32(v.(int))

	}

	//enforce_tls_requirements
	if v, ok := d.GetOk("enforce_tls_requirements"); ok && !isIntfNil(v) {

		createSpec.EnforceTlsRequirements = ves_io_schema_vs_profiles.EnableDisableChoice(ves_io_schema_vs_profiles.EnableDisableChoice_value[v.(string)])

	}

	//frame_size
	if v, ok := d.GetOk("frame_size"); ok && !isIntfNil(v) {

		createSpec.FrameSize =
			uint32(v.(int))

	}

	//header_table_size
	if v, ok := d.GetOk("header_table_size"); ok && !isIntfNil(v) {

		createSpec.HeaderTableSize =
			uint32(v.(int))

	}

	//include_content_length
	if v, ok := d.GetOk("include_content_length"); ok && !isIntfNil(v) {

		createSpec.IncludeContentLength = ves_io_schema_vs_profiles.EnableDisableChoice(ves_io_schema_vs_profiles.EnableDisableChoice_value[v.(string)])

	}

	//insert_header
	if v, ok := d.GetOk("insert_header"); ok && !isIntfNil(v) {

		createSpec.InsertHeader = ves_io_schema_vs_profiles.EnableDisableChoice(ves_io_schema_vs_profiles.EnableDisableChoice_value[v.(string)])

	}

	//insert_header_name
	if v, ok := d.GetOk("insert_header_name"); ok && !isIntfNil(v) {

		createSpec.InsertHeaderName =
			v.(string)

	}

	//receive_window
	if v, ok := d.GetOk("receive_window"); ok && !isIntfNil(v) {

		createSpec.ReceiveWindow =
			uint32(v.(int))

	}

	//user_spec
	if v, ok := d.GetOk("user_spec"); ok && !isIntfNil(v) {

		ls := make([]string, len(v.([]interface{})))
		for i, v := range v.([]interface{}) {
			if v == nil {
				return fmt.Errorf("please provide valid non-empty string value of field user_spec")
			}
			if str, ok := v.(string); ok {
				ls[i] = str
			}
		}
		createSpec.UserSpec = ls

	}

	//write_size
	if v, ok := d.GetOk("write_size"); ok && !isIntfNil(v) {

		createSpec.WriteSize =
			uint32(v.(int))

	}

	log.Printf("[DEBUG] Creating Volterra Http2Profile object with struct: %+v", createReq)

	createHttp2ProfileResp, err := client.CreateObject(context.Background(), ves_io_schema_vs_profiles_services_http2_profile.ObjectType, createReq)
	if err != nil {
		return fmt.Errorf("error creating Http2Profile: %s", err)
	}
	d.SetId(createHttp2ProfileResp.GetObjSystemMetadata().GetUid())

	return resourceVolterraHttp2ProfileRead(d, meta)
}

func resourceVolterraHttp2ProfileRead(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	resp, err := client.GetObject(context.Background(), ves_io_schema_vs_profiles_services_http2_profile.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] Http2Profile %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra Http2Profile %q: %s", d.Id(), err)
	}

	return setHttp2ProfileFields(client, d, resp)
}

func setHttp2ProfileFields(client *APIClient, d *schema.ResourceData, resp vesapi.GetObjectResponse) error {
	metadata := resp.GetObjMetadata()

	d.Set("annotations", metadata.GetAnnotations())

	d.Set("description", metadata.GetDescription())

	d.Set("disable", metadata.GetDisable())

	d.Set("labels", metadata.GetLabels())

	d.Set("name", metadata.GetName())

	d.Set("namespace", metadata.GetNamespace())

	return nil
}

// resourceVolterraHttp2ProfileUpdate updates Http2Profile resource
func resourceVolterraHttp2ProfileUpdate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	updateMeta := &ves_io_schema.ObjectReplaceMetaType{}
	updateSpec := &ves_io_schema_vs_profiles_services_http2_profile.ReplaceSpecType{}
	updateReq := &ves_io_schema_vs_profiles_services_http2_profile.ReplaceRequest{
		Metadata: updateMeta,
		Spec:     updateSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		updateMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		updateMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		updateMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		updateMeta.Namespace =
			v.(string)
	}

	if v, ok := d.GetOk("activation_modes"); ok && !isIntfNil(v) {

		ls := make([]string, len(v.([]interface{})))
		for i, v := range v.([]interface{}) {
			if v == nil {
				return fmt.Errorf("please provide valid non-empty string value of field activation_modes")
			}
			if str, ok := v.(string); ok {
				ls[i] = str
			}
		}
		updateSpec.ActivationModes = ls

	}

	if v, ok := d.GetOk("app_service"); ok && !isIntfNil(v) {

		updateSpec.AppService =
			v.(string)

	}

	if v, ok := d.GetOk("command"); ok && !isIntfNil(v) {

		updateSpec.Command =
			v.(string)

	}

	if v, ok := d.GetOk("concurrent_streams_per_connection"); ok && !isIntfNil(v) {

		updateSpec.ConcurrentStreamsPerConnection =
			uint32(v.(int))

	}

	if v, ok := d.GetOk("connection_idle_timeout"); ok && !isIntfNil(v) {

		updateSpec.ConnectionIdleTimeout =
			uint32(v.(int))

	}

	if v, ok := d.GetOk("enforce_tls_requirements"); ok && !isIntfNil(v) {

		updateSpec.EnforceTlsRequirements = ves_io_schema_vs_profiles.EnableDisableChoice(ves_io_schema_vs_profiles.EnableDisableChoice_value[v.(string)])

	}

	if v, ok := d.GetOk("frame_size"); ok && !isIntfNil(v) {

		updateSpec.FrameSize =
			uint32(v.(int))

	}

	if v, ok := d.GetOk("header_table_size"); ok && !isIntfNil(v) {

		updateSpec.HeaderTableSize =
			uint32(v.(int))

	}

	if v, ok := d.GetOk("include_content_length"); ok && !isIntfNil(v) {

		updateSpec.IncludeContentLength = ves_io_schema_vs_profiles.EnableDisableChoice(ves_io_schema_vs_profiles.EnableDisableChoice_value[v.(string)])

	}

	if v, ok := d.GetOk("insert_header"); ok && !isIntfNil(v) {

		updateSpec.InsertHeader = ves_io_schema_vs_profiles.EnableDisableChoice(ves_io_schema_vs_profiles.EnableDisableChoice_value[v.(string)])

	}

	if v, ok := d.GetOk("insert_header_name"); ok && !isIntfNil(v) {

		updateSpec.InsertHeaderName =
			v.(string)

	}

	if v, ok := d.GetOk("receive_window"); ok && !isIntfNil(v) {

		updateSpec.ReceiveWindow =
			uint32(v.(int))

	}

	if v, ok := d.GetOk("user_spec"); ok && !isIntfNil(v) {

		ls := make([]string, len(v.([]interface{})))
		for i, v := range v.([]interface{}) {
			if v == nil {
				return fmt.Errorf("please provide valid non-empty string value of field user_spec")
			}
			if str, ok := v.(string); ok {
				ls[i] = str
			}
		}
		updateSpec.UserSpec = ls

	}

	if v, ok := d.GetOk("write_size"); ok && !isIntfNil(v) {

		updateSpec.WriteSize =
			uint32(v.(int))

	}

	log.Printf("[DEBUG] Updating Volterra Http2Profile obj with struct: %+v", updateReq)

	err := client.ReplaceObject(context.Background(), ves_io_schema_vs_profiles_services_http2_profile.ObjectType, updateReq)
	if err != nil {
		return fmt.Errorf("error updating Http2Profile: %s", err)
	}

	return resourceVolterraHttp2ProfileRead(d, meta)
}

func resourceVolterraHttp2ProfileDelete(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	_, err := client.GetObject(context.Background(), ves_io_schema_vs_profiles_services_http2_profile.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] Http2Profile %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra Http2Profile before deleting %q: %s", d.Id(), err)
	}

	log.Printf("[DEBUG] Deleting Volterra Http2Profile obj with name %+v in namespace %+v", name, namespace)
	opts := []vesapi.CallOpt{
		vesapi.WithFailIfReferred(),
	}

	err = client.DeleteObject(context.Background(), ves_io_schema_vs_profiles_services_http2_profile.ObjectType, namespace, name, opts...)
	if err != nil {
		return fmt.Errorf("error deleting Http2Profile: %w", err)
	}
	return nil

}
